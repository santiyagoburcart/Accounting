# 📘 پنل حسابداری جنگو – Django Accounting Panel

یک پنل مدیریت مالی و حسابداری تحت وب، ساخته‌شده با جنگو، مجهز به داشبورد گرافیکی، پشتیبانی دوزبانه، سیستم مدیریت مشتریان، تراکنش‌ها و ابزارهای حرفه‌ای گزارش‌گیری بر اساس تقویم **شمسی**.

این پروژه با هدف ارائه یک پنل حسابداری سبک، کاربردی و قابل توسعه طراحی شده است که هم برای کسب‌وکارهای کوچک و هم پروژه‌های آموزشی بسیار مناسب است.

---

## 🚀 امکانات کلیدی

- 👤 **مدیریت مشتریان**  
  افزودن، ویرایش، حذف و مشاهده لیست مشتریان با فرم‌های کاربرپسند.

- 💰 **مدیریت تراکنش‌ها**  
  ثبت و مدیریت درآمدها، هزینه‌ها و محاسبه سود خالص.

- 📊 **داشبورد گرافیکی پیشرفته**
  - نمایش درآمد / هزینه / سود
  - نمودارهای مالی بر اساس روز، هفته و ماه
  - تقویم جلالی (شمسی)

- 🌐 **پشتیبانی دوزبانه (FA/EN)**  
  کل رابط کاربری و پیام‌ها از طریق فایل‌های ترجمه (`locale`) مدیریت می‌شود.

- 🎨 **تم روشن و تاریک**  
  قابل انتخاب توسط کاربر و ذخیره در پروفایل.

- 🧾 **ورود و خروج داده (CSV)**  
  ایمپورت و اکسپورت مشتریان.

- 🐳 **پشتیبانی از Docker**  
  دارای Dockerfile آماده برای استقرار سریع.

- 🔒 **سیستم پروفایل کاربری**  
  قابلیت تغییر رمز، ویرایش اطلاعات و آپلود آواتار.

---

# 🏗️ معماری پروژه (Project Architecture)

```
Accounting/
├── Accounting/              # تنظیمات، مسیرها، WSGI/ASGI
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py / asgi.py
│
├── dashboard/               # اپلیکیشن اصلی مدیریت، مشتریان و تراکنش‌ها
│   ├── models.py            # مدل‌های مالی، پروفایل، مشتریان
│   ├── views.py             # ویوهای داشبورد و صفحات مختلف
│   ├── forms.py             # فرم‌های مدیریت داده
│   ├── urls.py              # مسیرهای اپلیکیشن
│   ├── templates/           # قالب‌ها و صفحات HTML (داشبورد، ویجت‌ها)
│   ├── migrations/          # تاریخچه ساختار دیتابیس
│
├── locale/                  # ترجمه‌های فارسی و انگلیسی
│
├── media/                   # فایل‌های کاربر (آواتار و ...)
│
├── static/                  # فایل‌های فرانت‌اند (CSS, JS, Images)
│
├── Dockerfile               # فایل ساخت ایمیج داکر
├── manage.py
└── requirements.txt
```

---

# 📦 Tech Stack مورد استفاده

> این بخش را خودم براساس ساختار پروژه و نیازها تکمیل کردم 👇

### **Backend**
- Python 3.10+
- Django Framework
- Django ORM

### **Database**
- MySQL 8+
- utf8mb4 Unicode

### **Frontend**
- HTML5 / CSS3 / JS
- Bootstrap (یا قالب فرانت موجود)
- نمودارها (Chart.js یا ابزار مشابه)

### **Additional Tools**
- Babel / gettext — برای ترجمه
- Docker — جهت استقرار
- Virtualenv — محیط مجازی

---

# ⚙️ نصب و راه‌اندازی

## 1️⃣ پیش‌نیازها

- Python 3.10+
- MySQL 8+
- Git
- بسته‌های توسعه MySQL (برای نصب درایور Python)
- Docker (اختیاری)

---

## 2️⃣ مراحل نصب پروژه

### 1. کلون کردن پروژه

```bash
git clone <URL>
cd Accounting
```

### 2. ساخت محیط مجازی

```bash
python -m venv .venv
source .venv/bin/activate
```

### 3. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### 4. ساخت فایل `.env`

```bash
cp .env-sample .env
```

در فایل `.env`:

```ini
SECRET_KEY=...
DEBUG=True

DB_NAME=Accounting
DB_USER=root
DB_PASSWORD=....
DB_HOST=localhost
DB_PORT=3306
```

### 5. ساخت دیتابیس MySQL

```sql
CREATE DATABASE Accounting CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 6. اجرای مایگریشن‌ها

```bash
python manage.py migrate
```

### 7. کامپایل ترجمه‌ها

```bash
python manage.py compilemessages
```

### 8. ساخت ادمین

```bash
python manage.py createsuperuser
```

### 9. جمع‌آوری فایل‌های استاتیک

```bash
python manage.py collectstatic
```

### 10. اجرای سرور

```bash
python manage.py runserver
```

---

# 🐳 اجرای پروژه با Docker

### ساخت ایمیج:

```bash
docker build -t accounting-app .
```

### اجرای کانتینر:

```bash
docker run --env-file .env -p 8000:8000 accounting-app
```

---

# 🧭 Roadmap / TODO

این بخش را بر اساس معماری پروژه و قابلیت‌های فعلی هوشمندانه ساختم 👇

- [ ] افزودن سیستم نقش‌ها و سطح دسترسی‌ها (RBAC)  
- [ ] افزودن فیلترهای بیشتر برای گزارش‌های مالی  
- [ ] بهبود رابط کاربری داشبورد  
- [ ] ایجاد API REST برای موبایل  
- [ ] اضافه کردن تست‌های واحد (Unit Tests)  
- [ ] افزودن قابلیت ارسال فایل PDF گزارش‌ها  
- [ ] ساخت نسخه PWA (اپلیکیشن وب)

---

# ❤️ لایسنس

این پروژه می‌تواند تحت هر لایسنس دلخواه شما منتشر شود.  
*(در صورت تمایل برایت یک بخش لایسنس استاندارد هم اضافه می‌کنم.)*

---

# ✨ پایان

در صورت تمایل می‌توانم:

✅ لوگوی پروژه طراحی کنم  
✅ نسخه انگلیسی README بسازم  
✅ برای پروژه عکس گرافیکی Header طراحی کنم  
✅ بخش اسکرین‌شات آماده کنم (وقتی عکس‌ها را دادی)

