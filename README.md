````markdown
# پنل حسابداری جنگو (Django Accounting Panel) - نسخه v0.0.1 (Dockerized)

این پروژه یک سیستم مدیریت مالی و حسابداری پیشرفته است که برای مدیریت مشتریان، ثبت تراکنش‌ها (درآمد/هزینه) و گزارش‌گیری مالی طراحی شده است.
این نسخه از پروژه به صورت **کاملاً داکرایز شده (Dockerized)** بازنویسی شده است تا نصب، اجرا و مهاجرت آن روی هر سروری تنها با چند دستور ساده انجام شود.

---

## 🏗 معماری پروژه (Architecture)

این سیستم بر پایه معماری ** میکروسرویس (Microservices)** با استفاده از داکر طراحی شده است و شامل ۴ کانتینر اصلی است که در یک شبکه ایزوله با هم ارتباط دارند:

1.  **🌍 Web Server (Nginx):**
    * مدیریت درخواست‌های ورودی (Reverse Proxy).
    * مدیریت گواهی‌های امنیتی SSL (HTTPS).
    * سرو کردن فایل‌های استاتیک و مدیا.
2.  **🐍 Application (Django + Gunicorn):**
    * هسته اصلی نرم‌افزار که با پایتون و جنگو نوشته شده است.
    * اجرا توسط WSGI Server قدرتمند Gunicorn.
3.  **🗄 Database (MySQL 8):**
    * دیتابیس رابطه‌ای جهت ذخیره اطلاعات.
    * ایزوله شده در شبکه داخلی داکر (امنیت بالا).
4.  **🛠 DB Management (phpMyAdmin):**
    * رابط گرافیکی تحت وب برای مدیریت آسان دیتابیس.

---

## ✨ امکانات کلیدی

-   **مدیریت کامل مالی:** مشتریان، تراکنش‌ها، درآمدها و هزینه‌ها.
-   **داشبورد هوشمند:** گزارش‌های نموداری بر اساس تاریخ شمسی.
-   **چند زبانه:** پشتیبانی کامل از فارسی و انگلیسی (RTL/LTR).
-   **تم:** دارای حالت تیره (Dark Mode) و روشن.
-   **بک‌اپ خودکار:** سیستم بک‌اپ‌گیری هوشمند با قابلیت ارسال فایل به **تلگرام**.
-   **امنیت:** استفاده از متغیرهای محیطی (.env) و SSL.

---

## 🚀 راهنمای نصب و راه‌اندازی سریع

### پیش‌نیازها
تنها چیزی که روی سرور نیاز دارید:
-   **Docker** و **Docker Compose**
-   **Git**

### مراحل نصب (در ۵ دقیقه)

۱. **دریافت پروژه:**
   ```bash
   git clone [https://github.com/santiyagoburcart/Accounting.git](https://github.com/santiyagoburcart/Accounting.git)
   cd Accounting
````

۲. **تنظیم متغیرهای محیطی:**
از روی فایل نمونه یک کپی بگیرید و اطلاعات خود (رمز دیتابیس، توکن تلگرام و ...) را در آن وارد کنید.

```bash
cp .env-sample .env
nano .env
```

۳. **دریافت گواهی SSL (ضروری):**
قبل از روشن کردن داکر، باید برای دامنه‌های خود گواهی بگیرید (چون Nginx به آن‌ها نیاز دارد).

```bash
sudo certbot certonly --standalone -d hunaex.shop -d www.hunaex.shop
```

۴. **اجرا:**

```bash
docker-compose up -d --build
```

*سایت شما اکنون روی `https://hunaex.shop` در دسترس است.*

۵. **اعمال تغییرات دیتابیس (فقط بار اول):**

```bash
docker-compose exec web python manage.py migrate
docker-compose exec web python manage.py collectstatic --no-input
```

-----

## 🛠 جعبه ابزار مدیریت داکر (Cheat Sheet)

لیست دستورات کاربردی برای مدیریت سرور:

### مدیریت سرویس‌ها

| دستور | توضیحات |
| :--- | :--- |
| `docker-compose up -d --build` | ساخت مجدد و اجرای سرویس‌ها (بعد از تغییر کد) |
| `docker-compose down` | خاموش کردن کامل سرویس‌ها |
| `docker-compose ps` | مشاهده وضعیت کانتینرهای در حال اجرا |
| `docker-compose restart nginx` | ریستارت کردن فقط یک سرویس خاص |

### مشاهده لاگ‌ها (عیب‌یابی)

| دستور | توضیحات |
| :--- | :--- |
| `docker-compose logs -f` | مشاهده زنده تمام لاگ‌ها |
| `docker-compose logs -f web` | مشاهده لاگ‌های جنگو (خطاهای کدنویسی) |
| `docker-compose logs -f nginx` | مشاهده لاگ‌های وب‌سرور (خطاهای ۴۰۴ یا ۵۰۲) |

### دسترسی به داخل کانتینرها

| دستور | توضیحات |
| :--- | :--- |
| `docker-compose exec web bash` | ورود به ترمینال لینوکسِ کانتینر جنگو |
| `docker-compose exec db mysql -u root -p` | ورود مستقیم به کنسول MySQL |

-----

## 🤖 سیستم بک‌اپ‌گیری خودکار

این پروژه شامل یک اسکریپت هوشمند (`telegram_backup.sh`) است که:

1.  از دیتابیس MySQL داخل داکر نسخه پشتیبان (SQL) می‌گیرد.
2.  فایل را فشرده می‌کند.
3.  فایل را به ربات تلگرام شخصی شما ارسال می‌کند.
4.  فایل‌های قدیمی‌تر از ۷ روز را از سرور پاک می‌کند.

**تست دستی بک‌اپ:**

```bash
bash telegram_backup.sh
```

**تنظیم زمان‌بندی (Cron Job):**
برای اجرای خودکار در ساعت ۳ صبح، دستور `crontab -e` را بزنید و خط زیر را اضافه کنید:

```bash
0 3 * * * /bin/bash /root/Accounting/telegram_backup.sh
```

-----

## 📦 بازگردانی اطلاعات (Restore)

برای بازگردانی فایل بک‌اپ (مثلاً `backup.sql.gz`) به دیتابیس:

```bash
# روش خط فرمان
zcat backup.sql.gz | docker-compose exec -T db mysql -u root -pYOUR_PASSWORD Accounting
```

*یا از طریق آدرس `YourSite.com/phpmyadmin` وارد شوید و فایل را ایمپورت کنید.*

```
```